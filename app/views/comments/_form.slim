= simple_form_for comment, url: post_comments_path(post), data: { turbo: false } do |form|
  = form.input :content, as: :text, label: false
  = form.input :parent_id, as: :hidden, input_html: { value: parent_id }
  = form.button :submit, class: 'btn-primary mt-2'