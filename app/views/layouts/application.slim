doctype html
html
  head
    title RailsProject64
    meta name="viewport" content="width=device-width,initial-scale=1"
    = csrf_meta_tags
    = csp_meta_tag

    = stylesheet_link_tag "application", "data-turbo-track": "reload"
    = javascript_include_tag "application", "data-turbo-track": "reload", defer: true

  body.d-flex.flex-column.min-vh-100
    nav.navbar.navbar-expand-md.navbar-dark.bg-dark
      div.container-fluid
        = link_to 'Блог', root_path, class: 'navbar-brand'
        div.d-flex
          - if user_signed_in?
            = button_to 'Выйти', destroy_user_session_path, method: :delete, data: { confirm: 'Вы уверены?' }, class: 'btn btn-outline-success me-2'
          - else
            = link_to 'Войти', new_user_session_path, class: 'btn btn-outline-success me-2'
            = link_to 'Зарегистрироваться', new_user_registration_path, class: 'btn btn-outline-success'
    main.flex-shrink-0
      - if notice.present?
        p.alert.alert-info= notice
      - if alert.present?
        p.alert.alert-info= alert
      div.container
        = yield

    footer.footer.mt-auto.py-3.bg-light
      div.container
        span.text-muted
          |footer

